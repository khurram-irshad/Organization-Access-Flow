<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Organization Access Flow" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <%= stylesheet_link_tag "application", media: "all", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <nav style="padding: 10px; background-color: #f8f9fa; border-bottom: 1px solid #dee2e6;">
      <%= link_to "Home", root_path, style: "margin-right: 15px; text-decoration: none;" %>
      <%= link_to "Organizations", organizations_path, style: "margin-right: 15px; text-decoration: none;" %>
      
      <% if user_signed_in? %>
        <%= link_to "Contents", contents_path, style: "margin-right: 15px; text-decoration: none;" %>
        <span style="margin-right: 15px;">Welcome, <%= current_user.email %></span>
        <%= button_to "Sign Out", destroy_user_session_path, method: :delete, style: "display: inline;" %>
      <% else %>
        <%= link_to "Sign In", new_user_session_path, style: "margin-right: 15px; text-decoration: none;" %>
        <%= link_to "Sign Up", new_user_registration_path, style: "text-decoration: none;" %>
      <% end %>
    </nav>
    
    <div style="padding: 20px;">
      <% if flash[:alert] %>
        <div style="background-color: #f8d7da; color: #721c24; padding: 10px; border-radius: 5px; margin-bottom: 15px;">
          <%= flash[:alert] %>
        </div>
      <% end %>
      <% if flash[:notice] %>
        <div style="background-color: #d1edff; color: #0c5460; padding: 10px; border-radius: 5px; margin-bottom: 15px;">
          <%= flash[:notice] %>
        </div>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>